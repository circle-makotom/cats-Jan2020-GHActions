version: 2

jobs:
  test:
    docker:
      - image: circleci/node:latest-browsers

    steps:
      - checkout
      - run: npm i
      - run: npm run test

  deploy:
    docker:
      - image: circleci/python:latest

    steps:
      - checkout
      - run: w

workflows:
  version: 2
  test-and-deploy:
    jobs:
      - test
      - deploy:
          requires:
            - test
