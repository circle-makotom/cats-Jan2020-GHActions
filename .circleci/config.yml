version: 2

jobs:
  build:
    docker:
      - image: circleci/node:latest-browsers

    steps:
      - checkout
      - run: echo "Hello!" > ../hello
      - run: echo "Meow!" > meow
      - store_artifacts:
          path: ../hello
      - store_artifacts:
          path: meow

workflows:
  version: 2
  test-and-deploy:
    jobs:
      - build
